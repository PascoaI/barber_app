import { ModernPlaceholder } from '@/components/common/ModernPlaceholder';

export default function AdminBarbersPage() {
  return (
    <ModernPlaceholder
      title="Barbeiros"
      description="Modulo em migracao para App Router com Supabase Auth server-side."
    />
  );
}
